name: poetica
description: "AI-Powered Poetry Assistance Platform"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  sqflite: ^2.2.0
  path: ^1.8.3
  crypto: ^3.0.0
  bcrypt: ^1.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  # Uncomment this section if you are adding assets (like images or fonts) to your app
  # assets:
  #   - assets/images/
  #   - assets/fonts/
